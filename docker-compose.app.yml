services:
  api:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: gci-api
    env_file:
      - ./app/.env
    environment:
      HOST: 0.0.0.0
      PORT: 3001
    ports:
      - "3001:3001"
    volumes:
      - ./imagenes:/imagenes:ro
    restart: unless-stopped
